```sql
show databases;
use skillboxdb;
show tables
describe discussion_group
select * from discussion_group;
SELECT name FROM discussion_group;
SELECT name, group_id FROM discussion_group;
SELECT name group_name, group_id as id FROM discussion_group;
select 2 / (3 + 6) as result;
```