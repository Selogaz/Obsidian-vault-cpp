.aider*

# Транзиентный SQLite-индекс плагина hybrid-search — меняется при каждом запуске Obsidian
.obsidian-hybrid-search.db*

# Зависимости плагинов — платформенно-зависимые, не должны попадать в git
.obsidian/plugins/*/node_modules/
/.claude/
/skills/
