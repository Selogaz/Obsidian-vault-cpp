filters:
  and:
    - file.hasTag("contact")
    - or:
        - this.isCategory == false
        - or:
            - file.hasTag("category/" + this.file.name.replace(" ", "_"))
            - category.contains(link(this.file.name))
    - or:
        - this.contact_title == null
        - this.contact_title == ""
        - this.contact_title.length == 0
        - file.name.contains(this.contact_title)
    - or:
        - this.contact_type == null
        - this.contact_type == ""
        - this.contact_type.length == 0
        - file.tags.containsAny(this.contact_type)
    - or:
        - this.contact_relevant == null
        - this.contact_relevant == ""
        - this.contact_relevant.length == 0
        - this.contact_relevant.contains(relevant)
    - or:
        - this.contact_category == null
        - this.contact_category == ""
        - this.contact_category.length == 0
        - category.containsAny(this.contact_category)
    - or:
        - this.contact_meta == null
        - this.contact_meta == ""
        - this.contact_meta.length == 0
        - meta.containsAny(this.contact_meta)
    - or:
        - this.contact_problem == null
        - this.contact_problem == ""
        - this.contact_problem.length == 0
        - problem.containsAny(this.contact_problem)
    - or:
        - this.contact_production == null
        - this.contact_production == ""
        - this.contact_production.length == 0
        - production.containsAny(this.contact_production)
formulas:
  backlinks: |
    file.backlinks.length
  outlinks: file.links.length
  type: tags.filter(value.startsWith("contact/"))[0].replace(/^.*\//, '')
properties:
  formula.type:
    displayName: 🗄️ type
  file.name:
    displayName: 🔤 title
  note.addition:
    displayName: ➕
  note.relevant:
    displayName: 📌
  note.category:
    displayName: 🗺️ category
  note.meta:
    displayName: 🔎 meta
  note.problem:
    displayName: ⚡ problem
  note.production:
    displayName: 🏬 production
  formula.backlinks:
    displayName: ⬇️
  formula.outlinks:
    displayName: ⬆️
views:
  - type: table
    name: Table
    order:
      - file.name
      - addition
      - relevant
      - formula.type
      - category
      - meta
      - problem
      - production
      - formula.backlinks
      - formula.outlinks
    sort: []
    limit: 28
