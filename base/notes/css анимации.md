---
tags:
  - note/specific/code
  - category/css
aliases:
  - анимации
deck: obsidian::css
created: 2026-01-21T12:52:54+03:00
updated: 2026-01-21T15:18:50+03:00
---

**анимации**
—
# CSS анимация
Позволяет анимировать изменение значений CSS-свойств, используя опорные кадры. Анимация не будет работать без функции анимации и свойств, которые будут управлять этой анимацией (продолжительность, ускорение)

## Keyframes

![[css keyframes]]

## Свойства CSS анимации
[[css cвойства CSS анимации]]
- `animation-name` - определяет имя анимации ([[css keyframes|keyframes]]), в котором описывают кадры анимации
- `animation-duration` - время одного цикла анимации
- `animation-timing-function` - ускорение анимации
- `animation-delay` - задержка между временем загрузки элемента и временем начала анимации
- `animation-iteration-count` - количество повторений анимации
- `animation-direction` - возможность при каждом повторе анимации идти по альтернативному пути, либо сбросить все значения и повторить анимацию

Все свойства анимаций можно указать в одном свойстве, не указывая по отдельности:
```css
.box-animation {
	animation: slide 3s linear 1s infinite alternate;
}
```
## CSS transitions
![[css transitions]]
[[Практика по анимациям в CSS]]
