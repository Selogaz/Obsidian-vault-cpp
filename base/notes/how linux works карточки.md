---
tags:
  - note/specific/exact
  - category/linux
aliases: []
deck: obsidian::linux
icon: 📝
color: "#d0a570"
created: 2026-03-03T00:00:00+03:00
updated: 2026-03-03T13:49:49+03:00
---

**Три главных уровня ОС Linux**
—
1. Аппаратные средства (CPU, RAM, диски, сеть)
2. Ядро (kernel) — работает в kernel mode, имеет неограниченный доступ к CPU и RAM
3. Процессы — работают в user mode, в пространстве пользователя

**Пространство ядра (kernel space)**
—
Область памяти, доступная только ядру. Ядро работает в kernel mode с неограниченным доступом к аппаратным средствам.

**Пространство пользователя (user space)**
—
Область памяти, где работают пользовательские процессы. При ошибке процесса ядро может очистить последствия.

**Четыре основные задачи ядра Linux**
—
1. Управление процессами (распределение времени CPU)
2. Управление памятью (отслеживание состояния RAM)
3. Драйверы устройств (интерфейс между hardware и процессами)
4. Системные вызовы (syscalls) — API для взаимодействия процессов с ядром

**Стандартные потоки (stdin, stdout, stderr)**
—
- stdin (0) — стандартный ввод
- stdout (1) — стандартный вывод
- stderr (2) — стандартный вывод ошибок

**Права доступа в Linux**
—
- r (read) — чтение
- w (write) — запись
- x (execute) — выполнение

Категории: u (user/owner), g (group), o (others), a (all)

**Типы файлов устройств**
—
- Символьные устройства — побайтовая передача (терминалы, последовательные порты)
- Блочные устройства — блочная передача (диски)

**Именование устройств в Linux**
—
- Жёсткие диски: /dev/sd* (sda, sdb…)
- Разделы: /dev/sda1, /dev/sda2
- Терминалы: /dev/tty*
- Loop-устройства: /dev/loop*

**MBR vs GPT — разметка дисков**
—
- MBR: старый формат, до 2TB, 4 первичных раздела
- GPT: новый формат, до 8ZB, неограниченное количество разделов

**Основные файловые системы Linux**
—
- ext4: стандартная, журналирование, обратная совместимость
- xfs: высокая производительность, большие файлы
- btrfs: CoW, снимки, RAID встроенный
- swap: файл подкачки

**Что такое журналирование в ФС?**
—
Журнал записывает изменения до фактического выполнения — защита от сбоев.

**Компоненты LVM**
—
- PV (Physical Volume) — физический диск
- VG (Volume Group) — группа томов
- LV (Logical Volume) — логический том

**Процесс загрузки Linux**
—
1. BIOS/UEFI — проверка оборудования
2. Загрузчик (GRUB) — загружает ядро в память
3. Ядро — инициализирует оборудование, запускает init
4. Init (PID 1) — первый процесс в пространстве пользователя

**Systemd — основные понятия**
—
Современная система инициализации (PID 1). Управляет сервисами, mount points, таймерами.

**Типы юнитов systemd**
—
- service — сервис/демон
- socket — сокет
- target — группа юнитов (аналог runlevel)
- mount — точка монтирования
- timer — таймер
- path — отслеживание пути

**Основные targets systemd**
—
- multi-user.target — многопользовательский (текстовый)
- graphical.target — графический режим
- rescue.target — режим восстановления
- emergency.target — экстренный режим
- shutdown.target — остановка системы

**Основные команды systemctl**
—
```bash
systemctl start nginx
systemctl stop nginx
systemctl restart nginx
systemctl status nginx
systemctl enable nginx  # автозапуск
systemctl disable nginx
```

**Типы системного логирования**
—
- syslog — классический демон логирования
- rsyslog — расширенный syslog
- journald — systemd journal (бинарные логи)

**Формат cron**
—
минута час день месяц день_недели команда

**Файлы управления пользователями**
—
- /etc/passwd — учётные записи
- /etc/shadow — пароли (зашифрованные)
- /etc/group — группы

**Приоритеты процессов (nice)**
—
-20 (высший приоритет) до 19 (низший приоритет)

**Файловая система /proc**
—
Виртуальная ФС с информацией о ядре и процессах:
- /proc/cpuinfo — информация о CPU
- /proc/meminfo — информация о памяти
- /proc/uptime — время работы системы
- /proc/[pid]/ — информация о процессе
- /proc/sys/ — параметры ядра

**Основные команды мониторинга сети**
—
```bash
ip link show     # список интерфейсов
ip addr show    # IP-адреса
ip route show   # таблица маршрутизации
ss -tuln        # открытые порты
```

**Типы сетевых портов**
—
- Well-known ports: 0-1023
- Registered ports: 1024-49151
- Dynamic ports: 49152-65535

**Параметры shell-скриптов**
—
- $0 — имя скрипта
- $1, $2... — позиционные параметры
- $# — количество параметров
- $@ — все параметры
- $? — код возврата последней команды

**Переменные окружения**
—
- PATH — пути для исполняемых файлов
- HOME — домашняя директория
- USER — имя пользователя
- SHELL — текущая оболочка
- PWD — текущая директория
- LANG — локаль

**Файлы конфигурации bash**
—
- /etc/profile — системный профиль
- ~/.bash_profile — персональный (при login)
- ~/.bashrc — персональный (каждый запуск bash)
- ~/.bash_history — история команд

**Docker — основные команды**
—
```bash
docker run -it ubuntu bash    # запустить контейнер
docker ps                     # работающие контейнеры
docker ps -a                  # все контейнеры
docker images                 # образы
docker build -t myapp .       # сборка образа
docker stop container_id      # остановка
docker rm container_id        # удаление
```

**Namespace и Cgroup**
—
- Namespace — изоляция ресурсов (процессы, сеть, файловая система)
- Cgroup — контроль и ограничение ресурсов (CPU, память, I/O)

**Статические и динамические библиотеки**
—
- Статические (.a) — включаются в исполняемый файл при компиляции
- Динамические (.so) — загружаются при запуске программы

**Типы виртуализации**
—
- Аппаратная — KVM, Xen, VMware
- Контейнерная — Docker, Podman, LXC
- Паравиртуализация — модифицированная гостевая ОС

**Основные компоненты Kubernetes**
—
- Pod — минимальная единица
- Deployment — управление подами
- Service — сетевой доступ
- Ingress — HTTP маршрутизация

**Иерархия директорий FHS**
—
- /bin — основные программы
- /sbin — системные программы
- /etc — конфигурация
- /home — домашние директории
- /usr — пользовательские программы
- /var — изменяемые данные (логи, кеши)
- /tmp — временные файлы
- /proc — файловая система процессов
- /sys — sysfs
- /dev — файлы устройств

**Команды архивирования и сжатия**
—
- gzip — сжатие (.gz)
- tar — архивирование (.tar)
- tar + gzip = .tar.gz или .tgz
- zip — сжатие (.zip)

**Команды монтирования**
—
```bash
mount /dev/sda1 /mnt/data    # монтирование
umount /mnt/data             # размонтирование
mount                        # показать текущие
df -h                        # использование дисков
```
