---
tags:
  - status/todo
  - project/single
  - priority/b
  - category/obsidian
aliases: []
addition:
status: 🟥
priority: 🇧
category:
  - "[[obsidian]]"
meta:
problem:
creator:
production:
start: 2026-06-22
end:
url:
cover:
icon: ✏️
color: "#b87535"
created: 2026-06-22T00:01:12+03:00
updated: 2026-06-22T00:01:12+03:00
---

> [!toc]- Table of contents
> ```table-of-contents
> ```

> [!todo]- Tasks
> ```tasks
> path includes {{query.file.path}}
> group by heading
> hide task count
> ```

# Description

> [!info] Карта для ручной работы
> Это хранилище знаний — хабы, иерархии и раскладку делаю я сам. Заметка = карта и чек-лист, не задача для ИИ. Аудит проведён ассистентом 2026-06-22.

## 🩺 Диагноз

«Density» в статистике — обманчивая метрика: плотность графа механически падает при росте числа узлов (1009 заметок → ~1 млн возможных рёбер). Гнаться за числом бессмысленно. Реальные метрики:

| Метрика | Значение | Вывод |
| - | - | - |
| Атомарных заметок | 1009 | большой объём |
| Средн. исходящих ссылок | **1.18** | мало |
| Средн. входящих | 2.04 | норма |
| Заметок-тупиков (0 исходящих) | **557 (55%)** | корень проблемы |
| Без входящих | 24 (2%) | хабы уже работают |
| Полных сирот | 8 (1%) | можно игнорировать |

**Вывод:** новые хабы строить «про запас» нерентабельно — входящие и так в норме. Дефицит в боковых связях. Долг сконцентрирован: **351 заметка `java …` (~35% хранилища), из них 159 тупиков**, агрегаторами покрыто лишь ~33.

## 🎯 Проход 1 — структура для `java` (текущий)

Под категорией [[java]] создать 5 иерархий 🧬 и разложить по ним java-тупики **по смыслу заголовка**, не по ключевым словам. Формат — по образцу `base/_hierarchy/Создание объектов в Java.md`: `tags: [category/java, system/high/hierarchy]`, `category: [[java]]`, `icon: 🧬`, тело — буллеты `[[java X|X]]`.

Списки ниже — грубая авто-прикидка (покрыла 89/159), это **затравка, не финал**. Часть заметок попала не туда — сверяй глазами.

- [ ] **🧬 java — Spring и DI** (~29): `@Configuration и @Bean`, `@Profile`, `Autowired на сеттере`, `ContextLoadListener`, `PostConstruct и @PreDestroy`, `WebApplicationContext`, `aop в java`, `виды AOP`, `внедрение зависимости от другого бина`, `внедрение значений из property-файлов`, `именование бинов`, `интерфейсы InitializingBean и DisposableBean`, `контекст Spring`, `механизм автоконфигурации Spring Boot`, `плюсы Spring Boot`, `пояснения к pom.xml`, `пример application.yaml`
- [ ] **🧬 java — многопоточность** (~25): `ABA-проблема`, `CAS`, `ReentrantLock`, `Semaphore`, `cachedThreadPool`, `fixedThreadPool`, `scheduledThreadPool`, `singleThreadPool`, `green thread`, `stop the world`, `work-stealing алгоритм`, `fail-safe`, `локи в базах данных`, `локи и уровни изоляции транзакций`, `типы локов в БД`, `ACID`
- [ ] **🧬 java — коллекции и Stream API** (~11): `ArrayList`, `Hashtable`, `LinkedHashSet`, `String как ключ для HashMap`, `hash`, `свойства Stream API`
- [ ] **🧬 java — паттерны проектирования** (~17): `Decorator`, `Factory method`, `builder для продвинутых`, `fluent api vs builder`, `паттерн наблюдатель`, `антипаттерн`, `пример антипаттерна service locator`, `пример антипаттерна control freak` → сюда же вложить существующую [[Создание объектов в Java]] (🧬→🧬) и связать с мета-нотой [[Порождающие паттерны и идиомы]]
- [ ] **🧬 java — безопасность** (~7): `Base64`, `CSRF`, `HTTP-сессия`, `JWT`, `Авторизация`, `Аутентификация`, `почему String не подходит для пароля`
- [ ] Связать новые иерархии вверх (достижимость из хаба [[java]])

### ⚠️ Префикс `java` перегружен — это НЕ про Java-язык
Вынести отдельно (в `productivity`/`webdev` или в свою иерархию), не пихать в технические 🧬:
- Карьера/процессы: `java дейли`, `java джира`, `java груминг`, `java аутстаф`, `java гит-флоу`, `java коммерческая занятость`, `java agile`, `java middle …`
- Общий бэкенд/веб: `java HTTP`, `java HTTPS`, `java kafka offset`

## ⏭️ Следующие проходы (не сейчас)

- [ ] **Боковые связи заметка→заметка внутри java** — главный рычаг density (в каждую заметку 2–4 ссылки на близкие)
- [ ] **webdev**: тупики `web` (21), `css` (10), `js` (7); слить дубль иерархий `Vue.js` / `vue js`
- [ ] **294 одиночных тупика** и заметки-вопросы (`почему…`, `как…`) — закрывать привычкой при создании, а не разовым проходом

## ✅ Проверка

После раскладки прогнать аудит-скрипт и сравнить по java; открыть граф в Obsidian — java-узлы должны стянуться в кластеры вокруг 5 иерархий. На этом этапе закрываются входящие; исходящие тупики — задача прохода 2.
